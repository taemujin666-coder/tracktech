# PostgreSQL database dedicated to TrackTech. Do not point this at the DDE database.
TRACKTECH_DATABASE_URL=postgresql://localhost:5432/tracktech
# Set false after migrations and import are available on the local machine.
TRACKTECH_DEMO_MODE=true

